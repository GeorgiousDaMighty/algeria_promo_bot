services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - jet-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
volumes:
  jet-data:
